server:
  port: 7003
spring:
  datasource:
    url: jdbc:mysql://localhost:8036/db
    username: user
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update